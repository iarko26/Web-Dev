//Write a function that takes a string as an argument and returns its length.
function getsLength(st){
    let length;
    length=st.length
    return length;
    }
    console.log(getsLength("Javascript"));
/*
    let str="HTML";
    console.log(str.length);
    */